import React from "react";
import { View, Text } from "react-native";
import { useTailwind } from "tailwind-rn";

const ThemeScreen = ({}) => {
  const tw = useTailwind();

  return (
    <Text>en cours theme</Text>
  );
};

export default ThemeScreen;
