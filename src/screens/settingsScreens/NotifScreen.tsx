import React from "react";
import { View, Text } from "react-native";
import { useTailwind } from "tailwind-rn";

const NotifScreen = ({}) => {
  const tw = useTailwind();

  return (
    <Text>en cours notif</Text>
  );
};

export default NotifScreen;
